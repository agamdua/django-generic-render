.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install django_generic_render

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django_generic_render
    $ pip install django_generic_render
